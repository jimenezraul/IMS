<section class="card">
  <a href="/product/{{id}}">
    <header>{{name}}</header>

    <div class="card-body">
      <img src="/images/{{filename}}" alt="{{description}}">
      <p>Price: {{price}}</p>
    </div>
  </a>
</section>